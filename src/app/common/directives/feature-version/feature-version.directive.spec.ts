import { FeatureVersionDirective } from './feature-version.directive';

describe('AppFeatureVersionDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureVersionDirective();
    expect(directive).toBeTruthy();
  });
});
